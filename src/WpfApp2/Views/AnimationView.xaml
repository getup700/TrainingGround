<UserControl x:Class="WpfApp2.Views.AnimationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfApp2.Views"
             mc:Ignorable="d"
             Style="{StaticResource animation}"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Background="Black">
        <TextBlock HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontFamily="SegoeUI"
                   FontSize="100"
                   FontWeight="Thin"
                   Foreground="DimGray"
                   Text="Text Shimmer">
            <TextBlock.OpacityMask>
                <RadialGradientBrush x:Name="ShimmerBrush"
                                     Center=".5,.5"
                                     GradientOrigin=".5,.5"
                                     RadiusX=".43"
                                     RadiusY="2">
                    <GradientStop Color="Black" />
                    <GradientStop Offset=".5"
                                  Color="#6000" />
                    <GradientStop Offset="1"
                                  Color="#2000" />
                </RadialGradientBrush>
            </TextBlock.OpacityMask>
            <TextBlock.Triggers>
                <EventTrigger RoutedEvent="Loaded">
                    <BeginStoryboard>
                        <Storyboard>
                            <!-- 中心点水平移动动画 -->
                            <PointAnimation RepeatBehavior="Forever"
                                            Storyboard.TargetName="ShimmerBrush"
                                            Storyboard.TargetProperty="Center"
                                            From="-2,.5"
                                            To="3,.5"
                                            Duration="0:0:3.3" />
                            <!-- 渐变原点同步移动（增强光源真实感） -->
                            <PointAnimation RepeatBehavior="Forever"
                                            Storyboard.TargetName="ShimmerBrush"
                                            Storyboard.TargetProperty="GradientOrigin"
                                            From="-2,.5"
                                            To="3,.5"
                                            Duration="0:0:3.3" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </TextBlock.Triggers></TextBlock>
        <!--<Grid.Triggers>
            <EventTrigger RoutedEvent="Loaded">
                <BeginStoryboard>
                    <Storyboard>
                        <ColorAnimation Storyboard.TargetProperty="Background.(SolidColorBrush.Color)"
                                        From="Black"
                                        To="Gray"
                                        BeginTime="0:0:1"
                                        Duration="0:0:3"
                                        FillBehavior="HoldEnd" />
                    </Storyboard>
                </BeginStoryboard>
            </EventTrigger>
        </Grid.Triggers>-->
    </Grid>
</UserControl>
