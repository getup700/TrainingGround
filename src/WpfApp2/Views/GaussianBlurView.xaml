<UserControl
    x:Class="WpfApp2.Views.GaussianBlurView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctrl="clr-namespace:WpfApp2.CustomControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:WpfApp2.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    Style="{StaticResource animation}"
    mc:Ignorable="d">
    <Grid
        Background="Red">
        <!--<ctrl:GroundGlass
            Width="1000"
            Height="100"
            Margin="0"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            BackgroundImage="/Resources/VCG41N1886872183.png" />-->
        <Image x:Name="BackgroundImage"
               Source="pack://application:,,,/Resources/desktop.png" />
        <Border>
            <Border.Effect>
                <BlurEffect Radius="80"
                            RenderingBias="Performance" />
            </Border.Effect>
            <Border.Background>
                <VisualBrush Stretch="Uniform"
                             Visual="{Binding ElementName=BackgroundImage}" />
            </Border.Background>
        </Border>
    </Grid>
</UserControl>
