<UserControl x:Class="WpfAnimation.Views.LinearAnimationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfAnimation.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <UserControl.Resources>
        <Storyboard x:Key="linearSb">
            <!--<DoubleAnimation Duration="0:0:3"
                             From="100"
                             Storyboard.TargetName="textBlock"
                             Storyboard.TargetProperty="Width"
                             To="300" />
            <DoubleAnimation Duration="0:0:3"
                             From="100"
                             Storyboard.TargetName="textBlock"
                             Storyboard.TargetProperty="Height"
                             To="300" />-->
            <ThicknessAnimation Duration="0:0:3"
                                From="100 100 0 0"
                                To="0 0 0 0"
                                Storyboard.TargetName="textBlock"
                                Storyboard.TargetProperty="Margin" />
            <DoubleAnimation Duration="0:0:3"
                             From="0"
                             To="300"
                             Storyboard.TargetName="textBlock"
                             Storyboard.TargetProperty="(Border.RenderTransform).(RotateTransform.Angle)" />
        </Storyboard>
    </UserControl.Resources>
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Button.Click"
                      SourceName="btn">
            <BeginStoryboard Storyboard="{StaticResource linearSb}" />
        </EventTrigger>
    </UserControl.Triggers>
    <Grid>
        <StackPanel Orientation="Horizontal"
                    Height="30"
                    VerticalAlignment="Top">
            <Button Content="Start"
                    x:Name="btn"></Button>
        </StackPanel>
        <Canvas Margin="0 30 0 0">
            <TextBlock Text="33"
                       x:Name="textBlock"
                       Width="200"
                       Height="100"
                       Background="Yellow"
                       MouseLeftButtonDown="TextBlock_MouseLeftButtonDown">
                <TextBlock.RenderTransform>
                    <RotateTransform />
                </TextBlock.RenderTransform></TextBlock>
        </Canvas>
    </Grid>
</UserControl>
