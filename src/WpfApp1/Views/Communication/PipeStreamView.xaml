<UserControl x:Class="WpfApp1.Views.Communication.PipeStreamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfApp1.Views.Communication"
             xmlns:vm="clr-namespace:WpfApp1.ViewModels"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DataContext="{d:DesignInstance Type=vm:PipeStreamViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <StackPanel>
        <Label Content="{Binding Message}" />
        <Label Content="PipeStream" />
        <Button Content="Hello"
                Command="{Binding HelloCommand}" />
        <Button Content="Hi"
                Command="{Binding HiCommand}" />
    </StackPanel>
</UserControl>
