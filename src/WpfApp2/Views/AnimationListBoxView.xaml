<UserControl x:Class="WpfApp2.Views.AnimationListBoxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfApp2.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="450"
             d:DesignWidth="800"
             mc:Ignorable="d">
      <Grid Background="#202020">
        <ListBox x:Name="MyListBox"
                 Background="Transparent"
                 BorderThickness="0"
                 Foreground="White"
                 FontSize="16">
            <!--<ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <TranslateTransform X="100" /> --><!-- 初始偏移在右边 --><!--
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard>
                                    --><!-- 横向滑入 --><!--
                                    <DoubleAnimation Storyboard.TargetProperty="(RenderTransform).(TranslateTransform.X)"
                                                     From="100" To="0" Duration="0:0:0.4">
                                        --><!-- 用 BeginTime 设置延迟，利用 ItemsControl.AlternationIndex --><!--
                                        <DoubleAnimation.BeginTime>
                                            <MultiBinding Converter="{StaticResource DelayConverter}">
                                                <Binding RelativeSource="{RelativeSource Self}" Path="(ItemsControl.AlternationIndex)" />
                                            </MultiBinding>
                                        </DoubleAnimation.BeginTime>
                                    </DoubleAnimation>
                                    --><!-- 同时渐显 --><!--
                                    <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                     From="0" To="1" Duration="0:0:0.4" />
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.ItemContainerStyle>-->

            <!-- 为了能用 AlternationIndex 必须设置 AlternationCount -->
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            
            <ListBox.Items>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
                <ListBoxItem Content="asdfasdfasdfasfd"/>
            </ListBox.Items>
        </ListBox>
    </Grid>
</UserControl>
